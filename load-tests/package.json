{
  "name": "load-tests",
  "private": true,
  "scripts": {
    "test:chat": "artillery run flows/chat-flow.ts",
    "test:socket": "artillery run socket-flow.yml"
  },
  "devDependencies": {
    "artillery": "^2.0.0",
    "playwright": "^1.48.0"
  }
}
